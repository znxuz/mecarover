set(COMPONENT_SRCS "ros_interface.cpp")
set(COMPONENT_ADD_INCLUDEDIRS "." "$ENV{PROJECT_PATH}/include" "$ENV{PROJECT_PATH}/config")
set(COMPONENT_REQUIRES mrlogger Controller ros_lib)
register_component()
