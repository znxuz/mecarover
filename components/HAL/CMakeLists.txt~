set(COMPONENT_SRCS "ESP_MCPWM_DC.cc" "ESP_Counter.cc" "esp32_hal.cc")
set(COMPONENT_ADD_INCLUDEDIRS "." "$ENV{PROJECT_PATH}/include" "$ENV{PROJECT_PATH}/config")
set(COMPONENT_REQUIRES mrlogger)
register_component()
